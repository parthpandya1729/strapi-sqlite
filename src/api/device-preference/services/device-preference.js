'use strict';

/**
 * device-preference service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::device-preference.device-preference');
